{
  "buildCommand": "rm -rf node_modules dist dist-v2 .vercel/output && npm install --force --no-audit --no-fund && npm run build",
  "installCommand": "echo SKIP_INSTALL",
  "outputDirectory": "dist-v2",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/"
    }
  ]
}
