{
  "buildCommand": "echo BUILD-START-2026-05-17 && rm -rf node_modules dist .vercel/output && npm install --force --no-audit --no-fund && echo NPM-INSTALL-DONE && npm run build && echo BUILD-COMPLETE && ls -la dist/assets/ && touch dist/.fresh-build",
  "installCommand": "echo SKIP_INSTALL",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/:path*"
    },
    {
      "source": "/((?!api/).*)",
      "destination": "/"
    }
  ]
}
